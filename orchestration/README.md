# Phase 9 — Orchestration

Human-in-the-loop coordination suggestions.

This layer:
- proposes suggestions (`OrchestrationSuggestion`)
- never executes without explicit approval
- emits explainability logs for every suggestion

See `modules/orchestration/orchestration.service.ts`.

